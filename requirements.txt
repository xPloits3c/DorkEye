# 🦅 DorkEye Requirements
# Install with: pip install -r requirements.txt

# Core HTTP requests management
# urllib3 is also listed explicitly because it is imported directly
# in dorkeye.py and Tools/dorkeye_analyze.py (not only as a transitive dep)
requests>=2.32.3,<3.0.0
urllib3>=2.2.0,<3.0.0

# YAML configuration and template parsing
# 6.0.2+ fixes a yaml.load() security issue present in 6.0.1
PyYAML>=6.0.2,<7.0.0

# Advanced Terminal UI and formatting
rich>=13.7.0,<14.0.0

# DuckDuckGo Search API integration
# Pinned to v4.x — stable API, avoids breaking changes introduced in v5.x
ddgs>=4.0.0,<5.0.0
